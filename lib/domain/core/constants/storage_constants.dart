class StorageConstants {
  StorageConstants._privateConstructor();

  /// Key for storing [themeMode]
  /// Possible values are [light], [dark] and [system]
  static const String themeMode = 'themeMode';

  /// Key for storing theme name
  static const String theme = 'theme';
}
